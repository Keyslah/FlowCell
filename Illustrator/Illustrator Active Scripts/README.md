# Illustrator Active Scripts

This folder holds the managed installed Illustrator source copies that FlowTest owns.

- `Add Script` copies selected Illustrator sources here first.
- FlowTest then syncs runnable copies into Illustrator's runtime scripts folder.
- FlowCell state should keep this managed source path and store the synced runtime path separately.
