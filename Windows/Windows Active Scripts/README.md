# Windows Active Scripts

This folder holds the managed installed Windows script copies that FlowTest owns.

- `Add Script` copies selected Windows files and folders here.
- Windows buttons can execute directly from these managed active copies.
- FlowCell state should point here instead of the original picked path.
