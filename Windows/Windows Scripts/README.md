# Windows Scripts

This is the public downloadable Windows script library.

- Put shareable `.ps1`, `.cmd`, `.bat`, `.exe`, `.lnk`, `.vbs`, and `.ahk` sources here.
- `Add Script` can install from anywhere, but this is the default repo library folder.
- Managed installed copies belong in `Windows\Windows Active Scripts`.
