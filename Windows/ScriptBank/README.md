# Windows ScriptBank

This legacy share folder remains on disk so older references still resolve.

New public Windows source files should live in `Windows\Windows Scripts`.
Managed installed copies belong in `Windows\Windows Active Scripts`.
