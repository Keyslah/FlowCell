FlowCell Program Summary
Last updated: 2026-05-10
Revision note: FlowTest now uses a Tauri v2 + React + TypeScript frontend as the active FlowCell shell. The old PowerShell WPF shell remains on disk as inactive source, but the normal launcher now builds and starts `FlowCellFrontend\src-tauri\target\debug\flowcell_frontend.exe` through `FlowCell\helpers\Start-FlowCellFrontend.ps1` instead of running `FlowCellUI.ps1`. The new frontend keeps the same backend/runtime boundary: it loads `FlowCell\local\flowcell_state.json`, renders the far-left program rail, second panel rail, main tool grid, compact status area, Appearance tab, and emits typed command envelopes into `FlowCell\FlowCellCommandBackend.ps1`. Program tabs now participate in the same render-only style system as tool buttons: `FlowCellProgram` carries visual-only `style_group_id`, the program rail renders through the shared imported-skin/button-skin pipeline instead of a separate hardcoded visual path, the default imported style group is now bound to a real imported skin record, and the Appearance tab can assign style groups to program tabs directly so pasted imported HTML/CSS updates the live program buttons without a second custom rail skin path. The Appearance tab now also persists an app-wide `AppTheme` token set for shell background, chrome, rails, cards, dialogs, inputs, and host controls, the Tauri frontend now drives those surfaces through shared CSS variables so full-page theme edits apply live without changing execution behavior, the main-page host buttons now stay visible even when their auxiliary `Pop` selector is available, main-page button right-click now suppresses the native Chromium menu and exposes only `Rename`, `Edit Description`, and `Delete`, the main header/status surfaces have been stripped down to less diagnostic copy, and the Appearance surface now groups styling into Main Page and Pop-out sections, supports one-click saved style-group assignment for programs, panels, panel surface, cards, misc host buttons, pop-out regular buttons, and pop-out tool surfaces, lets the imported-skin editor save draft button HTML/CSS/SVG plus dedicated Card HTML/CSS/SVG with live button/card previews, now loads saved-skin drafts only when the selected saved record actually changes instead of clobbering the draft on every imported-skin array refresh, now routes preset picks to their canonical saved-skin slot when available so Glass Hover loads as one consistent preset instead of depending on the currently selected saved record, includes a built-in Glass Hover imported-skin/style-group option, and now exposes a dark Appearance-tab shell-theme action that switches page/card/chrome surfaces to `Signal Night` with white text plus a dedicated shell-text section for editing primary and secondary text colors; built-in imported pill labels now inherit that theme text color through an explicit theme-layer override so the program and panel button tags go white under the dark preset without rewriting saved button records. Card-like sections now read the dedicated card code path instead of stretching button markup across full surfaces, host-owned rail/chrome buttons no longer default to compact imported-skin rendering, the imported-skin editor now keeps its live previews visible instead of stretching the preview column to textarea height, uses fixed sample labels in that preview instead of borrowing the currently selected program label, no longer repeats section-level previews across the whole Appearance page, the built-in imported pill presets are now restored from the canonical code defaults on load so mutated built-in records stop drifting off-center or losing compact sizing, the main-window bind details now live as a compact info-rail block instead of a large center-surface card, the old Individual/Group pop-action cards have been removed from the main surface, compact tool chips are scaled back toward the original smaller tool-system footprint, compact host-wrapped tool chips now drop their extra outer padding so the clickable bounds and visible pill are effectively the same edge-to-edge surface, compact tool-popout alignment rows now use intrinsic `max-content` packed columns instead of fluid/fixed fill tracks, skinned compact tool buttons now strip the host-owned `is-active` / armed / live tile classes so only the visual skin owns the selected-state look, compact alignment/tool popouts now reserve a little top padding for the overlay controls and use taller compact chip heights so dense rows stay packed horizontally without looking crushed vertically, tool popouts no longer misuse the popout tool button style as a full-surface backdrop, which removes the oversized ghost surface labels and keeps the overlay controls from competing with the first tool row, compact grouped/individual regular-button popouts now use fixed dense tracks whose visible pill fills the saved compact cell instead of shrinking inside it, compact built-in imported pill skins now ship explicit compact padding/font rules so popout buttons stop using full-size pill spacing, compact ButtonCard wrappers now drop the old dark host square/background so imported popout buttons render as tight pills instead of tiles, tool-popout size clamps now still constrain dedicated compact tool surfaces such as alignment and Smart Axis, but ordinary panel and tool popouts now reopen at the actual saved bounds instead of re-inflating smaller saved sizes back to their default dimensions, compact panel/tool-popout buttons no longer inherit the persistent main-surface selected state, compact ButtonCard hosts now skip the focus-only selection mutation so the first click in panel popouts is not eaten by a focus-triggered re-render, the panel rail now exposes `Fan`, `Fan Options`, and `Pop` so `Fan` opens a transparent panel-fan popout surface whose actual window collapses down to the owner button when idle, expands only once the child pills are visible, now mounts as a bare transparent button-only surface instead of the regular slim popout shell, now explicitly applies a transparent native webview background color on panel-fan/floating-fanout create and reopen in addition to `transparent:true`/`shadow:false` while keeping those fanout windows immediately visible so the owner pill still appears on this desktop, now persists only the collapsed owner-footprint bounds for fanout reopen/layout restore, recomputes expanded fanout rectangles from stable untransformed owner/child pill metrics instead of transform or fanout-window resize feedback, can be repositioned by holding `Space` while dragging, and now reads per-panel `FanOptions` state so each panel can save `grid`, `radial`, or `half-radial` panel-fan layout plus owner-placement choices (`center`, `top`, `bottom`, and all four corners`) from its own dedicated `Fan Options` window, which now opens as a normal resizable framed window with the native close button and a scroll-safe content area so the lower placement row is reachable even with larger button skins, while `Pop` keeps the checked-button tool-popout fallback before full panel popout behavior. The built-in Glass Hover / Eggshell presets now include matching card-surface code so panel/card/pop-out surface assignments render as real framed panels rather than giant pill backdrops, and slim panel/tool popouts now suppress the native webview context menu, expose a single custom `Close` action on right-click, hide the old overlay `X` / drag chrome, keep panel-fan popouts as button-only surfaces without visible window controls, explicitly clear the `html`/`body`/`#root` backgrounds for panel-fan windows so the themed page slab no longer shows behind the floating buttons, let compact content sit flush to the popout edges instead of inside a second inset frame, and allow any slim popout to be moved by holding `Space` and left-clicking inside the popout content. That backend host remains the only active executor for scripts, macros, Blender bridge actions, and builtin callback requests: it validates envelopes, resolves targets, logs the command lifecycle, runs controller/bridge adapters, and returns structured results through the Tauri gateway. Style groups remain visual-only through `style_group_id`, imported HTML/CSS/SVG skins stay render-only inside host-owned wrappers, and owner/child fanout actions carry `request_id`, `owner_button_id`, and `child_slot_id` metadata without executing backend logic in the frontend. Additional panel/tool windows are now created through the Tauri v2 frontend `WebviewWindow` API with query-based window context so React surfaces mount in extra windows without the blank Rust-config path that was failing at runtime. The current code also grants explicit custom-window close/minimize/maximize permissions plus runtime `setAlwaysOnTop`, keeps the main FlowCell window forced non-topmost, creates panel/tool popouts as independent windows again so minimizing the main shell does not hide them, starts those extra windows in always-on-top mode before the per-program z-order loop takes over, hides popout taskbar buttons with `skipTaskbar`, reconciles and closes orphaned FlowCell webview windows from the main shell when they are not represented in saved state while leaving transient panel fan option windows alone, ignores stale minimized/offscreen popout bounds when reopening windows, reapplies saved bounds to already-open panel popouts during layout restore, persists panel/tool popout move+resize data and `Save Layout` snapshots in logical Tauri window coordinates so DPI-scaled desktops reopen at the same saved size and position, throttles move/resize persistence and saves those bounds without cross-window rebroadcast, limits state-sync listeners to the main window, restores the missing main-page host controls (`+ Add Program`, `+ Add Panel`, `Add Script`, `Add Macro`, `Save Layout`, `Load Layout`, and a compact bind area), renders panel/tool popouts as slim content-only shells except for the bare panel-fan/floating-fanout button surfaces, hides popout scrollbars, and now uses the official Tauri dialog plugin for file-picking flows instead of the earlier hidden PowerShell/WPF file picker path. Popout windows query the foreground Windows process through a small Tauri-side Win32 command and only keep themselves topmost while their owning FlowCell program's configured external process is foreground, or while that specific popout is the currently focused FlowCell window.
Fanout window note: panel `Fan` popouts and regular owner-button `Fanout` popouts now share one rewritten transparent undecorated Tauri-window model: each fanout window stays collapsed to the exact owner-pill footprint when idle, measures stable intrinsic owner/child pill sizes from a hidden untransformed DOM layer, computes one deterministic anchored grid rectangle per owner/direction, resizes/repositions the native window once per changed target bounds, keeps vertical/grid fanouts left-anchored to the owner instead of centering the owner over wide child grids, delays the expanded in-window geometry until that native move/resize has landed so the owner pill does not jump out from under the cursor, retains hover-close requests that happen during the native expand until the anchored owner/child pills are actually in place, renders child pills through the same owner-skin path so the expanded buttons match the initial owner styling, animates only the visible child pills inside the already-sized expanded window, makes transparent empty space pointer-inert through both CSS and native `setIgnoreCursorEvents`, collapses on hover-out or `Escape`, and persists only collapsed owner bounds. Regular owner fanouts still read the persisted per-owner direction state (`up`, `down`, `left`, `right`, `center`, `up-left`, `up-right`, `down-left`, `down-right`), and the backend command path for owner/child clicks is unchanged.
Current verified runtime status: main Tauri page launch, program selection, panel selection, panel-popout React mount, checked-button individual tool-popout mount, main-window state persistence, Snapshot backend dispatch, Alignment child-action backend dispatch, Flatten/Revolve child-action backend dispatch, and Smart Axis backend dispatch are confirmed by current frontend/gateway/backend logs. The shared Tauri invoke wrapper previously used snake_case argument keys for `save_state`, `open_panel_popout`, `close_panel_popout`, and `open_tool_popout`; that broke persisted panel selection, popout creation, and fanout saves until corrected to the Tauri v2 JavaScript-side camelCase argument names. Compound-tool owner buttons are now wired to the React/Tauri tool-popout path instead of intentionally routing through the legacy WPF helper scripts, and the backend host now safely handles missing `status_message` on alignment modifier child actions under PowerShell strict mode. Fanout assignment/rendering is now scoped to buttons in the same program panel as the owner, persists on the owner button in `flowcell_state.json`, and is now hidden from the main panel and compact panel-popout button surfaces so the main button grid is not decorated by fanout controls or expanded child clusters. The panel rail now keeps the existing checked-button tool-popout/full panel-popout behavior on `Pop`, while `Fan` opens the transparent panel-fan popout surface instead of previewing inside the main rail, `Fan Options` opens the dedicated per-panel options window for layout and placement, the rail actions now stack vertically so panel names keep their width, and the main-surface button hosts now render as plain shared host-skin buttons instead of showing the extra metadata-card chrome. Smart Axis now prefers the normalized `tool_option_state` payload returned through the gateway when updating `ToolOptionStates`, the backend host now correctly preserves `Modes.X/Y/Z` when converting its Smart Axis result into persisted `ToolOptionStates` instead of collapsing armed axes back to `NONE`, and the frontend baseline action now preserves the current axis/live arming configuration instead of letting baseline clicks rewrite it. The backend host is now spawned from the Tauri gateway with `CREATE_NO_WINDOW` plus `-NonInteractive`, and the Tauri frontend debug EXE itself now uses the Windows GUI subsystem so the normal launcher path no longer opens or keeps a visible console/PowerShell window attached to FlowCell. Older saved custom script buttons now have missing `command_id` values backfilled to valid script/macro command ids during state normalization, which repairs stale custom buttons such as the FlowTest snapshot variants that were emitting invalid command envelopes. `Save Layout` now reloads the latest saved `flowcell_state.json`, captures live open popout bounds directly from Tauri windows before snapshotting, and writes that refreshed state back to disk so saved layouts do not lose coordinates just because the main window held stale popout bounds. The custom popout close control now writes shared state before the window closes so tool/panel popouts do not immediately resurrect from stale main-window state. The earlier cross-window sync implementation caused every open popout to reload `flowcell_state.json` after Smart Axis saves and window-bound changes, which matched the reported lag/freezing; the current code removes that reload storm from popout windows, and high-frequency Smart Axis/alignment tool-state saves now stay local to the active window instead of rebroadcasting the full app state on each click. The frontend launcher now focuses an already-running Tauri window instead of killing and relaunching it when the taskbar launcher is invoked without a rebuild being required, the strict-mode launch path now normalizes the running-process lookup to an array so it no longer aborts before `Start-Process`, and the rebuild detector now only watches real frontend source inputs instead of generated `src-tauri\target` artifacts so normal taskbar relaunches stop taking the rebuild/restart path. The latest host-shell restoration, official-dialog, fanout-visibility, independent-popout, orphan-window cleanup, taskbar-hiding, launcher-restart, layout-refresh, popout-close, and transparent panel-fan shell fixes are build-validated in this repo, and the launcher has been restarted cleanly. Human visual smoke is still required for final confirmation that Blender popouts stay above Blender without repeated clicks, that the main FlowCell shell no longer feels topmost over Blender, that Add Script now opens the system picker correctly on this desktop, that the tiny popout close control works reliably in the live UI, and that panel-fan popouts feel right when hovered, space-dragged, resized, and switched between grid/radial/half-radial panel settings. On this specific machine, Windows taskbar grouping is also constrained by the current `HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced\TaskbarGlomLevel=2` setting (`Never combine`), so one-app grouped hover thumbnails for multiple FlowCell windows are not currently achievable without changing that OS-level taskbar mode.
2026-05-09 panel-fan follow-up: the per-panel `Fan Options` save action now submits through a real form, persists the chosen layout and placement even when the draft started from defaults, closes the options window directly after save, and the panel fan layout math now treats `center`, `top`, `bottom`, and the corner choices as true owner-anchor positions for `grid`, `radial`, and `half-radial` panel fans instead of collapsing those choices back into the old bottom-left-biased rectangle.
2026-05-09 panel-fan options follow-up: the `Fan Options` window now uses plain direct option/save buttons instead of the imported host-skin button wrapper for its controls, and the default Tauri capability now explicitly covers `panel-fan-options-*` labels so the window can be closed programmatically after save.
2026-05-09 fanout screen-fit follow-up: when a panel `Fan` or regular owner `Fanout` would overflow the screen, FlowTest now keeps the owner pill pinned to its collapsed screen footprint and shifts only the expanded child cluster inward before sizing the native window, so the owner no longer gets pushed off the cursor just because the child fan needed edge-fit adjustment.
2026-05-09 workspace/popout follow-up: the main panel section is now named `Workspace`, `Fan Options` was removed from the per-panel rail and moved into the main toolbar so it always targets the currently selected panel, and panel/fanout popout bounds saves now write to disk without re-rendering the live popout window that is being moved, resized, or collapsed.
2026-05-09 popout restore follow-up: regular panel/tool popouts now honor much smaller stored bounds instead of inflating back to layout-derived minimums on reopen, the compact panel-popout shell no longer auto-resizes itself after mount based on button count, and panel-fan fallback owner/child pills now render as full host-style buttons instead of bare text when imported skins are suppressed in the transparent fan window.
2026-05-09 panel-fan restore follow-up: panel-fan owner pills now use the selected panel name instead of the generic `Fan` label, the transparent panel-fan owner/child pills are back on the shared `HostSkinButton` render path instead of the custom fallback styling pass, and layout snapshot capture now reconstructs panel-fan tool-popout records from live window labels with state-aware panel/owner matching so `Save Layout` and `Load Layout` include panel fans again.
2026-05-09 grouped tool-popout restore follow-up: alignment and Smart Axis tool popouts no longer discard saved bounds during reopen, so layout loads now honor their stored coordinates instead of forcing those tools back to the generic default rectangle, and grouped tool-popout records now keep the chosen owner button first in `ButtonIds` so owner-based save/load matching stays stable for Smart Axis and other grouped tools.
2026-05-09 placement-stability follow-up: fan/popup placement now normalizes logical positions and sizes to device-pixel-stable values before applying them to native Tauri windows, existing panel-fan windows now get their saved collapsed placement actively re-applied during layout restore instead of merely being shown in-place, and layout snapshot capture now prefers the live collapsed fan bounds whenever the live fan window is already collapsed so a just-moved fan owner is not overwritten by an older stored collapsed position during `Save Layout`.
2026-05-09 restore-drift follow-up: panel popouts, regular tool popouts, and existing panel-fan/tool windows now suppress move/resize persistence briefly while the main window is programmatically reapplying saved native bounds during open/layout restore, the compact popout auto-size safety pass now marks its own resize as programmatic so it does not get re-saved as a user move, and `Load Layout` now ignores overlapping repeated clicks while one restore pass is already in flight.
2026-05-10 restore-tolerance follow-up: native popout/fan restore now reads the current logical Tauri window bounds before reapplying placement and skips the move/resize entirely when the window is already within about one physical pixel of the saved target, which prevents repeated layout loads from nudging existing windows sideways by DPI-rounding noise.
2026-05-10 coordinate-space follow-up: saved and live popout/fan window positions now read from Tauri `outerPosition()` while sizes continue to use `innerSize()`, so restore/open/save all use the same native coordinate space as `setPosition()` instead of mixing inner-position reads with outer-position writes on Windows.
2026-05-10 Windows Files follow-up: the current live FlowTest state/config now points Windows Git actions and program script folders at `D:\Dev\workspace\Codex\FlowTest\...` instead of the older `D:\Dev\workspace\Codex\flowcell\...` repo root, and the Tauri frontend still preserves saved absolute script targets exactly as stored so those live local paths remain stable after reload.
2026-05-10 Git action follow-up: the FlowTest-local `Windows\file_update_github.ps1` and `Windows\file_pull_github.ps1` files are now byte-matched copies of the known-good working scripts from the older FlowCell repo, so FlowTest no longer depends on that old repo path for Git actions; the AutoHotkey controller also now suppresses unchanged `last_action_status.txt` text when a script exits nonzero so stale status messages stop being replayed as if they were the current failure.
2026-05-10 Git hang follow-up: `FlowCellFrontend\.gitignore` now ignores frontend build output and dependency trees (`node_modules`, `dist`, and `src-tauri\target`) so the Windows `Update GitHub` action stops crawling generated Tauri/Vite artifacts before staging real source changes in the repo root.
2026-05-10 Git pipe follow-up: the FlowTest-local Windows Git scripts now drain redirected stdout and stderr concurrently with `ReadToEndAsync()` before waiting on the process results, which prevents `Update GitHub` and `Pull GitHub` from deadlocking when Git emits a large burst of warnings on one stream.
2026-05-10 Git warning follow-up: the FlowTest-local Windows Git scripts no longer echo the known CRLF/LF working-copy conversion warnings into successful Update/Pull popups; those line-ending notices are still filtered as warnings internally, but the user-facing status now stays focused on the actual Git outcome.
Guardrail note: Codex guardrails were added through `AGENTS.md` files. FlowTest-specific instructions protect the three-layer button architecture and keep visual skins render-only.
Responsive popout sizing note: slim popout layouts now use stretch tracks instead of intrinsic/fixed compact tracks, and the compact popout content chain now stretches to full window height, so compact popout buttons can consume both the available width and height instead of staying tiny and leaving dead space.

Maintenance rule
----------------
This file is the living summary for the FlowCell workspace. Any time FlowCell is updated, fixed, reorganized, or extended, update this file in the same change so it stays accurate.

Purpose
-------
FlowCell is a Windows desktop automation shell for creative and system workflows. It now uses a Tauri v2 + React + TypeScript frontend with an AutoHotkey v2 and PowerShell command backend to manage:

- program tabs
- editable panel tabs
- script and macro buttons
- hotkey bindings
- recorded macros
- panel popouts
- tool popouts
- popout layout snapshots
- Blender bridge actions

Open-source repository model
----------------------------
The repository is now arranged for public GitHub use. Tracked source, docs, examples, and tooling remain in the repo. Mutable user state, machine-specific settings, logs, and build artifacts are kept in ignored local storage.
FlowCell also now includes donation support through .github\FUNDING.yml, a README support badge link, and a Stripe Payment Link.
Contributor guidance now explicitly requires relative repo-path resolution instead of tracked machine-specific absolute paths.
README and contributor docs now lead with script folder rules, explicitly point Blender submissions to Blender\FlowCellButtons, and direct contributors toward GitHub Issues for bugs and GitHub Discussions for questions or early script proposals.
README now keeps the `How Blender buttons work` section short and direct: it points to the Codex prompt doc first, then gives only the core script requirements and valid-file outcome.
docs\blender-buttons.md is now intentionally short: it starts with the Codex conversion prompt, then keeps only a compact contract, Add Button flow, and main path list.
That Blender-button doc still covers FlowCell-root paths only, including FlowCellButtons, SupportScripts, ManagedActions, config files, Blender runtime filenames, and ScriptDump locations without embedding personal machine paths.
That Blender-button doc now states that FlowTest copies the selected script's code into the configured live Blender runtime file, which is `...\scripts\addons\flowtest_actions.py` for this sandbox.
That doc now keeps the custom-action structure guidance concise while still covering required entrypoint names, accepted callable signatures, prompt preservation, return shapes, and a minimal starter template.
Blender\FlowCellButtons\README.md is back to being a tiny folder-specific note that points readers at docs\blender-buttons.md.

Top-level folders:
- Blender: Blender integration files, including FlowCell button scripts, Blender support infrastructure, and a sanitized public config.
- FlowCell: the main desktop app, helper scripts, vendored dependencies, and ignored local runtime storage.
- Illustrator: public Illustrator script area.
- Illustrator\random: imported Illustrator script packs currently kept in-repo for evaluation/reference.
- Photoshop: public Photoshop script area.
- Windows: public Windows script area.
- docs: repository documentation.
- examples: public example configs with safe placeholders.
- releases: release-process notes only. No committed EXEs.
- tools: source-only tooling, including the optional C# launcher source.

Top-level launcher files:
- run.cmd: root entry point that delegates to FlowCell\run.cmd.
- run_hidden.vbs: hidden root launcher wrapper.
- run_backend_hidden.vbs: hidden root backend wrapper.

Main live files
---------------
- FlowCellFrontend\: active Tauri v2 + React + TypeScript desktop frontend.
- FlowCell\FlowCellUI.ps1: legacy PowerShell WPF shell source kept on disk but removed from the normal launch path.
- FlowCell\FlowCellCommandBackend.ps1: frontend-command/backend-execution host that validates command envelopes and owns script/macro/bridge dispatch.
- FlowCell\FlowCellBackend.ahk: AutoHotkey v2 backend controller.
- FlowCell\run.cmd: local app launcher for the Tauri frontend or backend execution.
- FlowCell\helpers\Start-FlowCellFrontend.ps1: frontend build-and-launch script for the active Tauri app.
- FlowCell\helpers\RecordMacro.ahk: recorded macro capture helper.
- FlowCell\helpers\RunRecordedMacro.ps1: recorded macro launcher helper.
- tools\launcher\FlowCellLauncher.cs: optional launcher source kept as source only.

UI architecture layers
----------------------
FlowCell UI now uses an explicit three-layer split for interactive button surfaces:

- State Layer: FlowCell state is the only source of truth for button identity, bindings, script targets, panel membership, popout/layout participation, selected tabs, persistence, the app-wide `AppTheme` token set, `StyleGroupId` / `style_group_id` assignments, and persisted compound-tool state such as per-owner alignment modifiers.
- State Layer now also persists button `command_id` values plus embedded tool option records in `ToolOptionStates`, so execution intent and tool settings stay in state instead of frontend-only globals.
- State Layer now resolves and validates button metadata before execution, including relative script-target normalization against the owning program configuration, and writes explicit `State resolved metadata...` plus `State validation...` log entries.
- Functional Host Layer: host-owned behavior for button command emission, compound child-action dispatch, selection checkboxes, drag/reorder surfaces, context menu integration, popout interactions, and persistence participation. Main-window buttons, panel popout buttons, grouped tool popouts, compound tool grids, individual tool popouts, embedded flatten/revolve tool actions, and hotkeys now all feed one shared command envelope path before backend execution.
- Functional Host Layer now rejects invalid state targets in the UI layer instead of handing broken button metadata to the backend, emits explicit `Frontend command emitted...` log entries, carries `request_id` plus owner/child fanout metadata in the shared envelope, and only applies UI-owned follow-up changes from backend results.
- Visual Skin Layer: render-only visual skin mounting inside host surfaces via style-group metadata lookup with sandboxed boundaries and automatic fallback to default FlowCell visuals if a style group is missing or invalid.
- Visual Skin Layer now writes explicit `Visual Skin rendered appearance...` log entries so appearance rendering stays observable without owning any behavior.
- Alignment tools now render as one responsive compound group with three axis rows (`Z`, `Y`, `X`) plus one full-width `Center Everything` row, and each interactive cell is a real host child action that gets its own `style_group_id`-driven visual skin instead of hardcoded widget colors.
- Smart Axis now stores its modes/live state in `ToolOptionStates` under the base owner button and renders as one grouped tool-popout surface (`Base`, `X`, `Y`, `Z`, `Live`) whose owner and fanout child buttons all emit the same typed envelope path through the Tauri gateway and backend command host.
- The dedicated backend command host is now the only active executor for FlowCell button actions. It writes `Backend command received...`, `State/payload validation...`, `Resolved execution target...`, `Bridge/runner execution result...`, and `Final user-visible status...` log entries in `FlowCell\local\logs\command_host.log`, while the Tauri gateway normalizes a stable frontend result shape (`ok`, `request_id`, `status`, `message`, `details`, `logs`, `exit_code`, `duration_ms`).

Program-tab and panel-tab strips now apply host-style item surfaces through shared tab item style generation so their visuals are aligned with the same host/skin model while behavior ownership remains in the host layer.

Local-only runtime storage
--------------------------
FlowCell now keeps mutable runtime data in FlowCell\local, which is ignored by Git.

Local runtime paths:
- FlowCell\local\bindings.ini: hotkeys, program tab metadata, and shortcut bindings.
- FlowCell\local\flowcell_state.json: selected program, selected panel, button layout, panel state, startup popout-only preference, popout state, and tool popout state.
- FlowCell\local\scan_state.ini: Illustrator scan cache/state.
- FlowCell\local\layouts: saved popout layout snapshots.
- FlowCell\local\panel_saves: saved panel exports.
- FlowCell\local\recorded_actions: recorded macro definitions.
- FlowCell\local\logs: UI, backend, launcher, and action status logs.
- FlowCell\local\logs\command_host.log: command-host validation, target resolution, runner/bridge execution, and final status log.
- FlowCell\local\private\local.settings.json: optional machine-specific settings such as private config paths.
- FlowCell\local\temp: generated temp files.
- FlowCell\local\bin: ignored local binaries such as the optional launcher EXE.

This change ensures existing panel configuration, hotkeys, bindings, popout layouts, saved panels, and other user changes remain local and should not be overwritten by GitHub updates.

Program folder rules
--------------------
Blender FlowCell button scripts belong in:
- Blender\FlowCellButtons

Blender support scripts belong in:
- Blender\SupportScripts

Blender sidebar helper add-ons belong in:
- Blender\AddonScripts

Managed Blender action source files belong in:
- Blender\ManagedActions

Illustrator and Windows user-facing scripts are now expected directly inside their program folder:
- Illustrator\*.jsx and Illustrator\*.js
- Windows\*.ps1, *.cmd, *.bat, *.vbs, *.ahk, or similar public launchers

Imported reference packs can also exist under:
- Illustrator\random

ScriptDump folders are reserved for loose, unfinished, downloaded, old, or experimental scripts and wrappers that should not be treated as normal FlowCell button candidates:
- Illustrator\ScriptDump
- Photoshop\ScriptDump
- Windows\ScriptDump
- Blender\ScriptDump
- Blender\FlowCellButtons\ScriptDump

Each ScriptDump folder uses only these standard subfolders:
- Downloaded
- Testing
- OldVersions

Photoshop repo-safe public scripts belong in:
- Photoshop\*

Illustrator internal helper scripts remain in:
- Illustrator\HelperScripts

Preferred filename prefixes:
- file_
- util_
- org_

These prefixes are for file organization and display cleanup only. They are not used for routing behavior, panel placement, or execution rules.

Preferred usage:
- org_ for organization actions such as Illustrator layer tools and Blender collection tools
- file_ for file-oriented scripts
- util_ for utility-style scripts

Script header rule:
- FlowCell-managed scripts in `FlowCell`, `Blender\FlowCellButtons`, `Windows`, and `Illustrator` now use a one-line `Description:` comment at the top of each script file.
- FlowCell UI reads this top description to populate concise button help text.
- `Blender\FlowCellButtons\org_snapshot.ps1` now explicitly describes snapshot behavior (copy selected Live objects into Snapshots as versioned `s#` duplicates) instead of a generic placeholder description.
- All scripts in `Blender\FlowCellButtons` now use action-accurate top descriptions that match their real behavior (bridge action, utility action, or full script behavior) instead of generic `Runs ...` placeholders.

GitHub collaboration guidance
-----------------------------
- GitHub Issues are for bugs, regressions, broken scripts, and concrete feature requests.
- GitHub Discussions are for questions, script ideas, workflow proposals, and early feedback before implementation.
- Script proposals should mention the intended target folder and preferred filename prefix.

Add Script behavior
-------------------
The main FlowCell panel Add Script button now:

- opens a file picker rooted in the current program folder
- allows selecting multiple scripts at once
- creates one button per selected script
- adds those buttons only to the currently selected panel
- for Blender program tabs, imported files selected outside `Blender\FlowCellButtons` are now automatically copied into `Blender\FlowCellButtons` first so the button target stays inside FlowCell's managed structure
- on Blender tabs, this control is labeled `Add Button` to match the one-click button workflow
- on Blender tabs, `Add Button` now runs a real installer flow for `.py` and `.ps1` selections: install/register a Blender bridge action, create or update a bridge wrapper in `Blender\FlowCellButtons`, add the new button to the currently selected FlowCell panel, and sync the Blender button list back into FlowCell state/layout files
- FlowTest now keeps that Blender Add Button lifecycle inside its own sandbox bridge at `C:\Users\aaron\AppData\Roaming\Blender Foundation\Blender\5.0\scripts\addons\blender_bridge_flowtest`, with separate `flowtest_actions.py`, `flowtest_bridge.py`, `flowtest_custom_actions.json`, and `flowtest_bridge_setup.json`
- Blender `Add Button` now reports when Blender must reload the add-on or restart before a newly installed action is callable
- Blender `Add Button` now only accepts Python files that expose `run_flowcell_action`, `main`, or a `perform_*` function, and rejects listener/bootstrap files instead of registering dead custom actions
- Blender `Add Button` now surfaces the first real installer failure reason when zero buttons are installed, so tool imports no longer stop at a generic `Check logs` message

Default button labels come from the filename with any leading file_, util_, or org_ prefix removed for display only.

For the built-in Blender tab, Add Script now roots to:
- Blender\FlowCellButtons
- Blender\SupportScripts

Legacy Blender add-on folders and the old repo-root Blender folder are treated as stale local paths and migrated forward to Blender\FlowCellButtons for button scripts.

ScriptDump behavior:
- normal default folder opening stays on the current program's main script folder
- manual navigation into a ScriptDump folder is still possible through the normal file picker
- FlowCell ignores scripts inside folders named exactly ScriptDump during normal button discovery, sync, and script-template setup paths

Startup and launcher model
--------------------------
The normal root launcher remains run.cmd at the repository root. It delegates to FlowCell\run.cmd.

FlowCell\run.cmd:
- launches FlowCell\helpers\Start-FlowCellFrontend.ps1 when started without backend arguments
- launches FlowCellBackend.ahk with AutoHotkey v2 when backend arguments are present
- writes launcher output into FlowCell\local\logs

FlowCell\helpers\Start-FlowCellFrontend.ps1:
- ensures frontend npm dependencies exist
- stops an already running `flowcell_frontend.exe` before rebuild/restart so the debug EXE is not left locked during `tauri build --debug`
- rebuilds the Tauri debug binary when frontend source files are newer than the executable
- starts the compiled `flowcell_frontend.exe`
- keeps the existing hidden backend launch path active through `run_backend_hidden.vbs`

The optional hidden launcher source is stored in tools\launcher\FlowCellLauncher.cs. Its built EXE is intentionally not part of the tracked repo.

UI behavior overview
--------------------
The active Tauri frontend provides:

- far-left vertical program rail
- second vertical panel rail
- script and macro buttons per panel
- host-owned button selection with render-only style skins
- panel popouts
- tool-popout window state backed by `ToolPopouts`
- saved popout bounds backed by FlowCell state
- compact bind/status area
- Appearance tab with a persisted app-wide theme option, grouped Main Page / Pop-out section styling, one-click saved section assignment, and an imported-skin editor that previews both button and card output before save
- owner-bound fanout assignment/editing in state with child command slots that render only when assigned
- frontend and gateway logging for state load, surface render, envelope emit, gateway receipt, backend launch, backend result receipt, and final UI status updates

FlowCellUI.ps1 remains available as legacy shell source only and should not receive new feature work.

Windows Git actions note:
- `Windows\file_update_github.ps1` stages, commits, and pushes the active Explorer Git repository to GitHub.
- `Windows\file_pull_github.ps1` uses the same Explorer/clipboard repo targeting model but only performs a clean-worktree `git fetch` plus `git pull --ff-only`, refusing dirty or diverged branches.

Shortcut display note:
- Bind Viewer, the shortcut picker list, the bind-area shortcut dropdown, and the macro/program shortcut readouts now translate internal AutoHotkey-style tokens into readable labels such as `Ctrl + Alt + K` while keeping the stored bind format unchanged.

Program tile note:
- Right-clicking a program tile on the main page now opens Rename Program and Delete Program actions. Delete removes that FlowCell program tab plus its panels, buttons, shortcuts, and saved live popout state from current local configuration.

Selected-tool popout note:
- `Pop Tools` now has only two modes: `Group` and `Individual`.
- `Group` opens checked regular buttons together in one responsive tool popout that reflows as the window is resized, matching panel-popout behavior more closely.
- `Individual` opens one popout per selected button.
- Special embedded tool controls that render many internal buttons, such as alignment tools and flatten/revolve tools, still pop out as their own individual windows even when other selected buttons are grouped.
- Grouped tool popouts now persist and restore as grouped windows instead of being split back into one window per button on restart.

The UI still persists popout-first workspace state, panel geometry, button order, selected tabs, button scale, and cluster membership, but those writes now stay under FlowCell\local instead of the tracked app folder.

Backend behavior overview
-------------------------
FlowCellBackend.ahk:

- loads bindings from FlowCell\local\bindings.ini
- loads recorded macros from FlowCell\local\recorded_actions
- writes action status to FlowCell\local\logs\last_action_status.txt
- writes controller logs and scan reports to FlowCell\local\logs
- runs Illustrator and Photoshop direct-script actions
- runs Windows and generic scripts
- handles Blender bridge actions
- registers hotkeys and action shortcuts

Blender integration
-------------------
Blender remains structured around:

- Blender\AddonScripts\refresh_flowcell_item_tab.py as a Blender-side Item-tab helper that offers both `refresh flowcell` and `refresh flowtest`, each reloading the matching live Blender add-on and restarting the matching desktop UI
- Blender\SupportScripts\Invoke-BlenderFlowCellAction.ps1 as the dispatcher support script
- Blender\SupportScripts\Sync-BlenderButtonsToFlowCell.ps1 as the sync/regeneration support script
- Blender\SupportScripts\Sync-BlenderCustomActionCode.ps1 as the live custom-code regeneration support script
- Blender\FlowCellButtons\org_*.ps1 user-facing button scripts for collections-style actions
- Blender\FlowCellButtons\file_*.ps1 user-facing button scripts for file-style actions
- Blender\FlowCellButtons\util_*.ps1 user-facing button scripts for utility-style actions
- Blender\config.json as the sanitized public default config
- FlowCell\local\private\blender.config.local.json as the machine-specific override for live bridge paths
- The live addon now keeps one shared live-tool registry/timer runner in `flowtest_bridge.py`; live tools register into that runner instead of creating per-tool modal operators or timers.
- Smart Axis Lock now appears in FlowCell as one grouped inline utility button (`Blender\FlowCellButtons\util_smart_axis_lock.ps1`) with internal baseline, X/Y/Z cycle, live toggle, and status controls that pop/move/delete together.
- The Blender sidebar panel for FlowTest actions is removed; Smart Axis Lock and similar dense controls should stay in FlowCell-hosted inline controls instead of Blender add-on UI panels.
- After editing the Blender bridge/add-on runtime, Blender must reload the add-on or restart before runtime verification reflects the new code.

Generated Blender button scripts resolve the dispatcher from a sibling SupportScripts folder instead of hardcoding a machine-local repo path.
Normal button discovery and Blender template setup must not treat Invoke-BlenderFlowCellAction.ps1 or Sync-BlenderButtonsToFlowCell.ps1 as user-facing button scripts.
The real Blender bridge runtime folder is the live AppData add-on path referenced by `automation.bridgeFolder`; it does not belong under `Blender\FlowCellButtons` or `Blender\SupportScripts` inside the repo.
The installed Blender addon runtime file `flowtest_actions.py` now identifies itself as `FlowTest` and includes a top-of-file button reference block that documents each button's behavior in concise terms.
Blender FlowCell button descriptions now avoid generic placeholders and include explicit behavior details (for example, `sort` now documents visible-to-Live, matching invisible-to-Snapshots `s#`, and remaining invisible-to-Trash `t#` behavior).
Blender wrapper scripts now consistently resolve the dispatcher from `Blender\SupportScripts\Invoke-BlenderFlowCellAction.ps1`, including `file_cura.ps1`, `file_orca.ps1`, and `util_cursor_center_hole.ps1`.
`Sync-BlenderButtonsToFlowCell.ps1` now resolves config `scriptPath` entries relative to the Blender project before protection/deletion and state import, so relative-path wrappers such as `file_cura.ps1`, `file_orca.ps1`, `util_alignment_tools.ps1`, `util_flatten_revolve_tools.ps1`, and `util_cursor_center_hole.ps1` are no longer mistakenly deleted during sync.
That sync path now fails loudly if a configured `scriptPath` target cannot be resolved to a real file, and it writes absolute script targets into FlowCell persistence for imported Blender buttons.
Blender `Add Button` installs custom action scripts into `Blender\ManagedActions` and registers them through `blender_bridge\flowcell_custom_actions.json` under the live Blender add-ons path.
The Blender bridge runtime now regenerates an auto-generated custom-action section inside the live `flowtest_actions.py` file from the currently registered Blender buttons, so only active custom button handlers remain in that file.
That live `flowtest_actions.py` regeneration now writes UTF-8 without a BOM, because Blender 5.0's add-on scanner only detects add-ons whose first line literally starts with `bl_info`, and a BOM made the FlowTest add-on disappear into Preferences > Missing Add-ons.
That custom-action regeneration path now also drops registry entries whose source Python file no longer exposes a supported FlowCell entrypoint, so stale invalid custom buttons cannot keep reappearing from the live Blender registry.
Deleting a Blender button now also removes its matching Blender config entry, prunes any orphaned custom-action registry entry and managed source file, regenerates the live `flowtest_actions.py` custom section, and re-syncs Blender wrappers/panels so removed buttons do not come back later.
Every wrapper in `Blender\FlowCellButtons` now starts with a `Description:` line plus a source map (`Source Python File`, action/function, and start line) followed by a commented copy of the real Python logic, while still dispatching through `Blender\SupportScripts\Invoke-BlenderFlowCellAction.ps1`.
`Sync-BlenderButtonsToFlowCell.ps1` now preserves and regenerates that wrapper metadata block for built-in Blender action wrappers, including blank-line spacing between the description, source-map fields, and commented Python logic.
That Blender sync path now also skips popout-only layout snapshots that do not embed a full `Programs` state payload, so routine Blender button syncs no longer emit false layout warnings.
Built-in Blender action wrappers and the live Blender config now prefer an authoritative action-description map during install/sync, so stale generic tooltips like `Run the Blender sort action...` are replaced with behavior-specific text in both wrapper headers and launcher metadata.
`Blender\AddonScripts\refresh_flowcell_item_tab.py` now provides a Blender `Item` sidebar panel labeled `FlowCell` with both `refresh flowcell` and `refresh flowtest` buttons; each one reads the matching workspace Blender config, disables and re-enables the matching live bridge add-on module, and relaunches that workspace through its own `run_hidden.vbs`.

The public example for Blender config now belongs in:
- examples\Blender\config.example.json

Blender bridge requests prefer the configured bridge root, but FlowCell now also falls back to installed addon bridge folders under the user's AppData Blender addons path when a stale local bridge path no longer matches the live addon.
Blender\ScriptBank now includes `custom_lithophane_tool.py`, a FlowCell-ready single-file lithophane action whose default `run_flowcell_action` opens an image picker, creates the DPI-sized plane, and completes the lithophane in one click while still exposing separate top-level `perform_*` helpers.
Blender wrapper install/sync writes now retry briefly on Windows file locks, downgrade locked-wrapper metadata refreshes to warnings, and surface a normal Add Button status error instead of hard-failing the whole FlowCell window when one `.ps1` wrapper is temporarily in use.
FlowCell now includes explicit Blender import helpers at `Blender\FlowCellButtons\Import-FlowCellObjIntoBlender.ps1` and `Blender\FlowCellButtons\Import-FlowCellPngAsLithophane.ps1`; these bridge import actions and write the `ResultPath` key/value context expected by `FlowCellBackend.ahk` so `blender obj` and `blender litho` can report deterministic status.
FlowTest also restores the live Blender utility and slicer wrappers in `Blender\FlowCellButtons`, including `file_cura.ps1`, `file_orca.ps1`, `util_alignment_tools.ps1`, `util_flatten_revolve_tools.ps1`, `util_cursor_center_hole.ps1`, and the Smart Axis helper set.

Repo-safe examples
------------------
Public example configs now live under examples:

- examples\FlowCell\bindings.example.ini
- examples\FlowCell\flowcell_state.example.json
- examples\FlowCell\local.settings.example.json
- examples\Blender\config.example.json
- examples\Windows\windows.env.example

These examples intentionally avoid personal paths and secrets.

Windows helper override model
-----------------------------
Some Windows helper scripts still depend on machine-local tools, but the tracked repo no longer hardcodes those paths.

- fix_this_folder_from_explorer.ps1 now resolves the Codex skill path from CODEX_HOME or %USERPROFILE%\.codex.
- monitor layout helper scripts now expect FLOWCELL_DUMMY_MONITOR_SCRIPT locally and can optionally use FLOWCELL_DUMMY_MONITOR_TARGET_DISPLAY.
- sync_recent_creative_shortcuts.ps1 now uses local environment overrides for recent-root and watch-root locations, with neutral per-user defaults instead of personal drive paths.
- file_update_github.ps1 now prefers the active Explorer repo, then clipboard, then the FlowCell repo itself, and finally a folder picker to find a Git repo. It stages changes, prompts for a commit message when needed, pushes the current branch to GitHub, and shows a visible success or failure message.
- monitor layout launcher entry scripts now resolve their sibling dialog scripts from $PSScriptRoot instead of a fixed machine path.
- monitor layout wrappers now share Windows\DummyMonitorHelpers.ps1, auto-discover dummy_monitor_toggle.py from FLOWCELL_DUMMY_MONITOR_SCRIPT or the sibling Codex workspace, and write explicit FlowCell status text instead of falling back to a generic script success line.
- the Save Layout and Use Layout wrappers no longer depend on the Python helper writing JSON only when stdout is a TTY; they now show success from the requested layout name after a verified zero exit code.
- the Save Layout and Use Layout entry scripts now run their STA dialog helpers synchronously, so FlowCell waits for the real result and can surface actual monitor-layout failures instead of instant launcher success.
- the one-shot Dummy button restore path now prefers the fresh session snapshot from the last toggle before falling back to the saved active layout profile, avoiding stale `main` profile restores when the display topology has changed since that profile was saved.
- the shared Windows dummy-monitor helper now validates the Python interpreter before use and prefers a working `python.exe` over a broken `py.exe` launcher, preventing false button failures caused by a stale Python launcher registration.
- FlowCell now canonicalizes side-specific hotkey text like `<^<+F2` into one shared stored bind such as `^+F2`, so the backend cannot keep duplicate left/right modifier variants alive and the UI stops showing the wrong modifier side for the active shortcut.
- FlowCell now repairs orphaned script-button `BindingId` values by matching the current script target and program tab during UI sync, which fixes stale Utility-panel button binds like the Windows `Dummy` entry after a binding is recreated.
- FlowCell host tab visual style now uses typed WPF values for host tab setters/template values (`Padding`, `Margin`, `BorderThickness`, `CornerRadius`, and content alignment enums), preventing startup failures caused by string-based style and template conversion.

Illustrator 3D test scripts now resolve their helper macro wrappers from the repo's FlowCell\helpers folder relative to the script location instead of a user-specific codexapp path.

UI-dependent Windows scripts now launch through the backend STA path when their filename indicates dialogs or interactive prompts, including file_update_github.ps1, fix_this_folder_from_explorer.ps1, and the monitor layout dialog helpers.

FlowCell's backend runner now respects child process exit codes and preserves script-written status output for CLI/button launches, so Update GitHub reports real verified Git success or failure instead of a generic launcher result.
The Update GitHub script also disables PowerShell's native-command stderr escalation around Git calls so normal push progress output does not get misreported as a failure after a successful push.
The Update GitHub flow now opens a topmost commit-message dialog that shows the resolved repo path, writes clearer intermediate status text while waiting/committing, and gets a longer FlowCell script timeout so interactive git pushes are not killed after the generic 120-second window.
Slim FlowCell popouts now use a definite viewport-height shell with explicit `minmax(0, 1fr)` content tracks, so compact tool layouts can consume extra vertical space instead of collapsing to intrinsic content height.
Panel `Fan` popouts now compute their expanded bounds from the real owner/child button geometry instead of holding onto a seeded rectangle, then collapse back to the owner pill footprint and persist only that collapsed anchor position so the transparent fan window no longer keeps a large idle slab over Blender.
Panel `Fan` popouts now measure the visible owner/child pills directly, reopen from the saved anchor position with a small collapsed footprint instead of restoring stale saved width/height, and lay child buttons out in a tight centered grid above the owner so the transparent native window wraps the visible buttons more closely and stops clipping the top row during hover-open.
Panel `Fan` popouts now also create their Tauri window with `shadow: false` alongside `decorations: false` / `transparent: true`, restore the fan pill labels after the earlier wrapper CSS regression, and skip popout-bound persistence until the measured fan metrics are ready so raw expanded rectangles are less likely to be written back as collapsed bounds.
Tool popouts now reuse an already-open Tauri window with the same label by repositioning/focusing it instead of closing and recreating it; this avoids the old-window close handler deleting the saved `ToolPopouts` record during reopen, which was causing panel-fan windows to disappear immediately after opening.
Panel `Fan` popouts now render the owner pill at the collapsed window origin while idle and only shift it into the expanded anchored position while open, so shrinking the native window back to the owner footprint no longer clips the owner button offscreen and makes the fan appear to vanish.
Panel `Fan` geometry now measures stable intrinsic owner/child pill sizes from the rendered DOM instead of transformed animation bounds, which prevents the native panel-fan window from repeatedly resizing itself during hover-open and reduces the flash-open/instant-collapse behavior caused by the window chasing animated measurements.
Panel `Fan` measurement no longer subscribes to the popout window's own resize event; the layout now tracks actual pill-size changes through DOM observers only, which avoids panel-fan bounds recalculating in response to the very resize animation they are trying to drive.
Fanout implementation note: the old layered panel-fan/floating-fanout experiments have been replaced by one deterministic model. Hidden measurement pills supply stable intrinsic owner/child sizes, both fanout variants compute absolute child rectangles in window coordinates, native bounds are derived only from collapsed owner bounds plus those measured rectangles, expanded windows no longer feed their own resize events back into layout, child visibility is separated from native expand/collapse so only the pills animate, and transparent hover/pointer retention now comes only from the visible owner/child pills instead of the empty expanded window area.
Panel `Fan` popout move/resize persistence now reads the live open-state and metrics refs instead of stale effect-closure values. That prevents the first open-cycle resize events from being misread as a collapsed panel-fan move, which was forcing collapsed-bound saves, state reloads, and immediate child-grid disappearance right after the buttons appeared.
Panel `Fan` now notifies the parent popout shell about open/close state directly from the hover handlers instead of waiting for a post-render effect. That makes the main popout window mark itself open before the first native resize events fire, so the opening resize no longer gets handled as a collapsed move/save cycle that immediately tears the child grid back down.
Panel `Fan` no longer persists live move/resize events at all while its Tauri popout exists. The panel-fan window had still been writing collapsed-bound move saves during open, which forced state reloads and made the child buttons vanish right after appearing. Removing live panel-fan move/resize persistence stops that self-reload loop.
Panel `Fan` reopen calls from the main window no longer reapply the small stored collapsed placement onto an already-open panel-fan Tauri window. Existing panel-fan windows are now only shown/focused on reopen, while their live size is left under the panel-fan hover-expansion logic. The default collapsed seed size is also tighter, so the initial hover block stays closer to the visible owner pill.
Panel `Fan` now ignores stale saved width/height when reopening its Tauri window and reuses only the saved anchor position, while the React fan root itself stays exactly owner-pill sized while closed and switches to the measured expanded bounds only after hover-open. The panel-fan popout surface also stopped centering a full-window root, so the idle transparent hover block no longer stretches wider than the visible owner pill just because an older run left a larger stored rectangle behind.
Panel `Fan` expanded layout no longer forces a half-screen transparent hover surface. The popout now grows only to the measured owner-plus-child grid size, keeps the owner anchored from a padded top-left origin inside that natural box, and only switches the React hover/root surface into expanded geometry once the child buttons are actually visible. That removes the oversized empty hover block that was appearing before the children rendered.
Panel `Fan` Tauri windows no longer start hidden on creation. That hidden-start experiment prevented some new panel-fan popouts from reliably reaching the measured sync path, so panel-fan windows are visible again immediately while still using the tighter collapsed seed sizing and the measured follow-up sync.
Panel `Fan` collapsed owner rendering now reuses the real compact shared button-skin path with the rail `Fan` label and misc-host style assignment instead of a panel-name placeholder bubble, so the transparent hover window can contract to the actual visible owner-button footprint.
Panel `Fan` child buttons now also render and measure through the shared compact button-skin path instead of custom panel-fan bubble placeholders, and the tighter native seed bounds now start close to that real owner size before hover-open expands the window to the measured visible child grid.
Panel `Fan` owner rendering now also mirrors the real rail-button fallback path when no `main-misc` style group is assigned, instead of forcing the default glass-card skin inside the popout. That prevents the collapsed transparent fan window from showing a fake dark slab when the live rail button is still using the plain `panel-rail__popout` fallback.
Panel `Fan` hover-open no longer forces the Tauri popout window to take focus during the resize/reposition sync, so the hover-only fan surface can expand without stealing focus mid-open and feeling inert.
Panel `Fan` transparent popouts now force a plain visible host-button fallback whenever the assigned owner/child style group uses an imported button skin. That avoids backdrop-filter/glass imported skins turning into unreadable dark slabs in the transparent always-on-top panel-fan window while preserving the normal imported-skin path on non-transparent main and regular popout surfaces.
Panel `Fan` is back to closed-by-default hover behavior. The panel-fan cluster no longer auto-opens on mount, no longer runs the temporary preview-open/auto-close experiment, and no longer uses click-to-open as a fallback. Main-window startup restore for panel and tool popouts also now runs only once per frontend startup instead of replaying on later state-sync renders, which stops panel-fan windows from being reopened over and over while they are in use.

Release model
-------------
The repository is source-only by default.

- EXE files and build artifacts are ignored.
- Built binaries are not committed to the repo.
- The optional launcher source is tracked under tools\launcher.
- Release binaries should be produced locally and later distributed through GitHub Releases.

Current maintenance expectations
--------------------------------
- Keep public source in tracked repo folders.
- Keep mutable runtime data in FlowCell\local.
- Keep Blender FlowCell button scripts in Blender\FlowCellButtons only.
- Keep Blender support scripts in Blender\SupportScripts.
- Keep Illustrator and Windows user-facing scripts directly in their program folder.
- Keep Photoshop repo-safe public scripts directly in Photoshop.
- Keep Illustrator\HelperScripts for internal non-button helper scripts.
- Prefer filename prefixes for file organization only.
- Update this summary whenever the repository structure or FlowCell behavior changes.

