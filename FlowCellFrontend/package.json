{
  "name": "flowcell-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "patch:slicer": "node -e \"const fs=require('node:fs');const p='src-tauri/src/main.rs';let s=fs.readFileSync(p,'utf8');const old=['    let single_instance_handoff = executable_has_running_window(&executable)','        && executable_supports_single_instance_arg(&executable);','','    let mut command = Command::new(&executable);','    if single_instance_handoff {','        command.arg(SLICER_SINGLE_INSTANCE_ARG);','    }','    command.args(&model_paths);'].join('\\n');const neu=['    let was_running = executable_has_running_window(&executable);','','    let mut command = Command::new(&executable);','    command.args(&model_paths);'].join('\\n');let n=s.split(old).join(neu).split('    if single_instance_handoff {').join('    if was_running {');if(n!==s){fs.writeFileSync(p,n);console.log('Patched slicer launcher.')}\"",
    "predev": "npm run patch:slicer",
    "prebuild": "npm run patch:slicer",
    "pretauri": "npm run patch:slicer",
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^6.0.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.11"
  }
}
