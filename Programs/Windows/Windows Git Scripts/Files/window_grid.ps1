#requires -version 5.1
# FlowCell Window Grid launcher.
# FlowCell intercepts this button by name and opens the Window Grid overlay
# (all open FlowCell windows as cards, grouped by program and panel). If it ever
# runs directly without interception it intentionally does nothing.
Write-Host 'Open the Window Grid from FlowCell.'
