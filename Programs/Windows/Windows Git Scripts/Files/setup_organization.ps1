# Description: Setup Organization profiles in FlowCell's native webview.
# FlowCell intercepts this marker before script execution and opens the
# organization-setup Tauri/React window. This file intentionally has no UI.
Write-Output 'Setup Organization is handled by the FlowCell organization-setup window.'
