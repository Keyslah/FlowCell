# Windows Git Scripts

Tracked shared Windows script sources live here, organized by panel subfolder. Add Script reads from here by default but writes runnable copies to `Panels/<Panel>/` and flat durable copies to `Windows Local Scripts/`.
