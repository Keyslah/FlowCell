# Unsupported Codex Micro actions

No runnable entry or manifest was created for any action below.

| Action name | Interfaces inspected | Exact reason it cannot currently be implemented | Verified interface needed |
| --- | --- | --- | --- |
| Open Thread 1 | Installed `Ctrl+1` command, Codex Micro HID `AG00`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The installed command opens the first **visible** task slot. Default Codex Micro slots are dynamically sourced from recent/pinned/priority tasks, while OpenMicro slots are process-local PTYs. Neither is a permanent numbered task, and this catalog has no stable slot-to-UUID mapping. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Open Thread 2 | Installed `Ctrl+2` command, Codex Micro HID `AG01`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The second visible slot is dynamic and is not a permanent task identity. A positional shortcut would violate the stable-selection requirement. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Open Thread 3 | Installed `Ctrl+3` command, Codex Micro HID `AG02`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The third visible slot is dynamic and is not a permanent task identity. A positional shortcut would violate the stable-selection requirement. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Open Thread 4 | Installed `Ctrl+4` command, Codex Micro HID `AG03`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The fourth visible slot is dynamic and is not a permanent task identity. A positional shortcut would violate the stable-selection requirement. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Open Thread 5 | Installed `Ctrl+5` command, Codex Micro HID `AG04`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The fifth visible slot is dynamic and is not a permanent task identity. A positional shortcut would violate the stable-selection requirement. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Open Thread 6 | Installed `Ctrl+6` command, Codex Micro HID `AG05`, current local state/SQLite, `codex://threads/<UUID>`, and OpenMicro `focus_session` | The sixth visible slot is dynamic and is not a permanent task identity. A positional shortcut would violate the stable-selection requirement. | A supported external Codex API that exposes a stable named-slot-to-thread-ID mapping, or an explicit portable user configuration that supplies the target UUID. |
| Approve Changes | Installed `approval.approve` command and contextual Enter binding, live UI Automation tree, desktop app-server schema/process, local state/logs, and OpenMicro's PTY Enter action | No live pending approval/changes surface was available to prove an exact accessible approval control. Enter is also task submission and is unsafe when focus/context differs. The desktop's stdio app-server cannot be attached to externally, and OpenMicro controls a separate CLI PTY. | A supported external desktop command/IPC scoped to the current pending approval, or a stable accessible approval control that can be observed and directly exercised in the installed build. |
| Reject Changes | Installed `approval.decline` command and contextual Escape binding, live UI Automation tree, desktop app-server schema/process, local state/logs, and OpenMicro's PTY Escape action | No live pending approval/changes surface was available to prove an exact accessible rejection control. Escape is also cancel/dismiss/interrupt and is unsafe when focus/context differs. The desktop's stdio app-server cannot be attached to externally, and OpenMicro controls a separate CLI PTY. | A supported external desktop command/IPC scoped to the current pending approval, or a stable accessible rejection control that can be observed and directly exercised in the installed build. |
