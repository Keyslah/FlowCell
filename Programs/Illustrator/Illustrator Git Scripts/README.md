# Illustrator Git Scripts

Tracked shared Illustrator script sources live here, organized by panel subfolder. Add Script reads from here by default but writes runnable copies to `Panels/<Panel>/` and flat durable copies to `Illustrator Local Scripts/`.
