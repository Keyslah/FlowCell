# Photoshop Git Scripts

Tracked shared Photoshop script sources live here, organized by panel subfolder. Add Script reads from here by default but writes runnable copies to `Panels/<Panel>/` and flat durable copies to `Photoshop Local Scripts/`.
