# Blender Git Scripts

Tracked shared Blender script sources live here, organized by panel subfolder. Add Button reads from here by default but writes runnable copies to `Panels/<Panel>/` and flat durable copies to `Blender Local Scripts/`.
