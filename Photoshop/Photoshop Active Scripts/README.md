# Photoshop Active Scripts

This folder holds the managed installed Photoshop source copies that FlowTest owns.

- `Add Script` copies selected Photoshop sources here first.
- FlowTest then syncs runnable copies into Photoshop's runtime Scripts folder.
- FlowCell state should keep this managed source path and store the synced runtime path separately.
