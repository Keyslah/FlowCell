# Blender ScriptBank

This legacy share folder remains on disk so older links do not break.

New public Blender downloads should live in `Blender\Blender Scripts`.
Managed installed source copies belong in `Blender\Blender Active Scripts`.
Bridge-managed runtime code still lives under `ManagedActions`, and clickable wrappers still live under `FlowCellButtons`.
