# Organization Profile Update

Windows Files now has two new organization buttons.

Setup Organization saves named profiles.
New Organization reads the active saved profile.
The old Organize Folder path was not changed.
